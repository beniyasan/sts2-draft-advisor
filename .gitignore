# Game DLLs — never commit, they are MegaCrit's assets. Drop them in ./lib locally.
lib/
*.dll

bin/
obj/
build/
.env

overlay/node_modules/
overlay/dist/

# Licensed Live2D assets — fetched by scripts/fetch-model.mjs, never committed.
overlay/assets/
overlay/vendor/
overlay/overlay.log
